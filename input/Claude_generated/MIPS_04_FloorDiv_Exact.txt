void main()
{
    PrintInt(10 / 2);
    PrintInt((0 - 10) / 2);
    PrintInt(10 / (0 - 2));
    PrintInt((0 - 10) / (0 - 2));
    PrintInt(0 / 5);
    PrintInt(0 / (0 - 5));
    PrintInt(1 / 1);
    PrintInt((0 - 1) / (0 - 1));
}
